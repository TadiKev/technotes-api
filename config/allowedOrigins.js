// Define an array of allowed origins (domains) that are permitted to access server resources
const allowedOrigins = [
        
    'https://www.technotes.onrender.com',  
         
];

// Export the array so it can be used in the CORS configuration
module.exports = allowedOrigins;
